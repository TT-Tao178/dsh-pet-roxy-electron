# 桌面宠物项目文档（设计 · 开发 · 扩展 · 接口 · 边界 · 功能 合并版）

> **文档版本**：v1.0　**对应程序版本**：v0.3.4　**最后更新**：2026-09-06
> **适用范围**：本文档同时适用于两套变体（同一套代码）——
> ① `dsh-pet-roxy-electron`（原版，角色：洛琪希 / Roxy）
> ② `dsh-pet-electron`（变体，角色：爱莉希雅 / Elysia，打包名 `elysia-pet`）
> **与其它文档的关系**：本文件是**项目级全集**（设计/开发/扩展/接口/边界/功能）；`dsh-pet-roxy-electron-会话总结.md` 是**会话交接与复盘**（状态、待办、防错规则 §11），二者需同步维护。

---

## 目录

- [0. 文档说明](#0-文档说明)
- [1. 项目概览](#1-项目概览)
- [2. 设计文档](#2-设计文档)
- [3. 功能文档](#3-功能文档)
- [4. 接口文档](#4-接口文档)
- [5. 边界文档](#5-边界文档)
- [6. 开发文档](#6-开发文档)
- [7. 扩展文档](#7-扩展文档)
- [8. 附录](#8-附录)

---

## 0. 文档说明

| 项 | 说明 |
|---|---|
| 目标读者 | 后续接手的开发者 / AI 对话（先读本文第 1–2 节建立全局观，再按需查 4/5 节） |
| 维护约定 | 改代码后同步更新：①本节版本号与"对应程序版本"；②受影响章节；③会话交接文档的状态/待办 |
| 双变体同步 | 结构性改动需同步到两个项目；变体专属内容（角色图、名称、数据目录）只在"变体差异表"体现 |
| 约定 | 文中 `%HOME%` 指用户数据目录（默认 `~/.roxy-desktop-pet`，变体为 `~/.elysia-pet`，可用 `ROXY_HOME` 覆盖） |

---

## 1. 项目概览

### 1.1 定位与形态

Windows 桌面宠物：一个**透明、无边框、置顶、可拖拽**的悬浮窗，里面住着一位角色；配套一个**独立的设置/统计窗口**和**系统托盘菜单**。数据全部本地化（无服务端、无联网），资源与逻辑随安装包分发。

- 运行形态：双击 `start.bat`（开发态）或安装包安装后启动（发布态）
- 平台：Windows 10/11 x64（Electron 33）
- 打包：electron-builder 25 + NSIS（可选安装目录、桌面快捷方式、卸载器）

### 1.2 变体差异表

| 维度 | 原版（Roxy） | 变体（Elysia） |
|---|---|---|
| 项目目录 | `D:\Workspace-1\code\dsh-pet-roxy-electron` | `D:\Workspace-1\code\dsh-pet-electron` |
| `package.json name` | `roxy-desktop-pet` | `elysia-pet` |
| `productName` / exe | `Roxy Desktop Pet` / `Roxy Desktop Pet.exe` | `elysia-pet` / `elysia-pet.exe` |
| `appId` | `com.roxy.desktop.pet` | `com.elysia.pet` |
| 安装包命名 | `Roxy-Desktop-Pet-Setup-${version}.exe` | `elysia-pet-Setup-${version}.exe` |
| 用户数据目录 | `~/.roxy-desktop-pet` | `~/.elysia-pet` |
| 角色图 | `assets/roxy0~4.png`（洛琪希） | 同文件名，内容为爱莉希雅（`0.png~4.png` 换入） |
| 图标 | `ico/favicon.ico` | 同路径（爱莉希雅 `0_256x256.ico`） |
| 显示文案 | Roxy / 洛琪希 | Elysia / 爱莉希雅 |
| **不变的部分** | 全部代码逻辑、文件名 `roxy*.png`、IPC 通道、HTTP 路由、`ROXY_HOME`/`ROXY_SELFTEST` 环境变量名、`client/roxy-widget.js` 文件名 | 同 |

> ⚠️ 变体的"内部名"刻意保留 `roxy`（文件名、环境变量、DOM 键），只有**外部可见标识**（应用名、数据目录、文案、图标、图片内容）改变——这样两套代码可继续互相借鉴补丁。

### 1.3 技术栈

| 层 | 技术 | 版本/说明 |
|---|---|---|
| 壳 | Electron | ^33.2.0（实际打包 33.4.11） |
| 打包 | electron-builder | ^25.1.8，target `nsis` x64 |
| 主进程 | Node ESM（`"type": "module"`） | `main.js` |
| 预加载 | CommonJS | `preload.cjs`（**必须 CJS，沙箱下 ESM preload 静默失效**） |
| 渲染层 | 原生 JS + CSS（IIFE，无框架） | `client/roxy-widget.js` |
| 本地服务 | Node `http` 模块 | `lib/server.js`，仅监听 `127.0.0.1`，随机端口 |
| 数据 | JSON 文件 + 原子写 | `%HOME%/*.json`、`%HOME%/uploads/` |

### 1.4 目录结构与文件清单

```
<项目根>/
├── main.js               # 主进程：窗口/托盘/IPC/自启/崩溃日志/自检
├── preload.cjs           # contextBridge 暴露 window.petDesktop（含 IPC 载荷净化）
├── index.html            # 渲染入口（透明背景，仅加载 /widget.js）
├── client/roxy-widget.js # 页面侧全部逻辑与样式（宠物 UI + 设置/统计面板，UI 模式复用）
├── lib/server.js         # 本地 http 服务：配置/偏好/任务/图片 + 静态下发
├── assets/               # roxy-config.json + roxy0~4.png（角色图与默认配置）
├── ico/favicon.ico       # 托盘/窗口/exe/安装包图标
├── scripts/smoke-server.mjs # 服务端冒烟测试（19 项，不依赖 Electron）
├── installer.nsh         # 自定义 NSIS 宏（卸载后清理残留安装目录）
├── start.bat             # 一键开发启动（镜像 + 完整性检测）
├── add-autostart.bat / remove-autostart.bat # 旧版自启开关（现由设置/托盘开关替代）
├── package.json          # 脚本 + electron-builder 配置（内嵌 build 字段）
├── dist/                 # 打包产物（安装包 + win-unpacked，已 gitignore）
└── .electron-cache/ .builder-cache/ .npm-cache/ .build-tmp/ # 本地缓存（已 gitignore）
```

---

## 2. 设计文档

### 2.1 总体架构

```
┌─────────────────────────── Electron 主进程 (main.js) ───────────────────────────┐
│  本地 http 服务 (lib/server.js, 127.0.0.1:随机端口)                              │
│    ├─ /config /prefs /tasks /image /user-image  ←─ 数据读写（JSON 文件 + 上传目录）│
│    └─ /  /widget.js                             ←─ 静态下发                      │
│  窗口：宠物窗(透明/无框/置顶) + UI 窗(640×780 居中，设置与统计)                    │
│  托盘：Tray + 右键菜单（穿透/透明度/置顶/自启/打开设置/退出）                       │
│  IPC：窗口控制通道 + pet-run 广播通道 + 状态同步通道                              │
└───────▲────────────────────────────▲───────────────────────────▲───────────────┘
        │ preload (contextBridge)    │ HTTP (fetch)              │ Tray/Menu
┌───────┴───────────────┐   ┌────────┴──────────────┐   ┌────────┴───────────────┐
│ 宠物窗 renderer        │   │ UI 窗 renderer         │   │ 用户（托盘/右键/拖拽）  │
│ roxy-widget.js(宠物)   │   │ roxy-widget.js(UI模式) │   └────────────────────────┘
│ 有 root：图像+气泡      │   │ root=null：仅面板      │
└────────────────────────┘   └───────────────────────┘
```

### 2.2 窗口模型（三种运行形态，务必区分）

| 形态 | 加载地址 | 关键事实 | 该形态下代码要注意 |
|---|---|---|---|
| **宠物窗** | `http://127.0.0.1:port/` | 透明、`frame:false`、`alwaysOnTop('screen-saver')`、可拖拽、`skipTaskbar` | `root` 存在；是**所有偏好的唯一执行者** |
| **UI 窗**（设置/统计） | `.../?ui=settings` / `?ui=tasks` | 640×780 居中、`autoHideMenuBar` + `setMenu(null)`、单例（已存在则仅 focus）、关闭面板即关窗 | **`root` 为 null**（不建宠物 DOM）；只能"编辑 + 发指令"，禁止直接操作宠物 DOM |
| 纯浏览器调试 | 直接开 `http://127.0.0.1:port/` | 无 `window.petDesktop`（`desktop = null`） | 所有 IPC 调用需 `if (desktop && desktop.xxx)` 兜底 |

> 🔴 历史教训：v0.3.0 之前，行为页在 UI 窗里直接 `root.style/classList` → 抛异常导致"大小/自启/动画/保存全失效"。**新代码必须按上表判断形态。**

### 2.3 三条通信通路（职责严格分离）

| 通路 | 载体 | 用途 | 方向 |
|---|---|---|---|
| **HTTP** | `fetch` → `lib/server.js` | 持久化数据：配置、偏好、任务、图片 | renderer → 主进程内服务 |
| **IPC 直连** | `preload` → `ipcRenderer.send/invoke` | 窗口级控制：移动、缩放、穿透、透明度、置顶、自启、开关 UI 窗、退出 | renderer → 主进程 |
| **pet-run 广播** | `desktop.sendRun(payload)` → 主进程 `broadcastRun`（排除发送者） | 跨窗口指令：设置窗改设置后让宠物窗执行；托盘 → 宠物窗 | renderer ↔ renderer（经主进程中转） |

**指令类型（`payload.type`，由宠物窗 `handleRun` 消费）**：
`mouse-through` · `opacity` · `top-mode` · `autostart` · `through-state`（主进程下发） · `preview` · `reload-config` · `scale`（`save:true` 才落盘） · `anim` · `mirror`

### 2.4 状态与单一事实源

- **偏好（prefs）**：唯一权威副本在服务器 `%HOME%/config.json`；宠物窗启动 `loadConfig()` 后应用到界面；设置窗只负责编辑，改动物即时发 `pet-run` 指令，点"保存"后 `PUT /prefs` 并 `sendRun({type:'reload-config'})` 让宠物窗以保存值为准。
- **托盘勾选状态**：主进程 `trayState` 由渲染层 `desktop.stateSync({topMode,opacity,mouseThrough,autostart})` 同步（宠物窗启动/改设置/托盘指令后都会同步）。
- **窗口位置**：`%HOME%/window.json`，`move`/`resize` 事件 400ms 防抖写入。
- 规则：**同一状态只有一个执行者**（宠物窗），其余全部走指令。

### 2.5 渲染与变换模型

| 概念 | 实现 | 说明 |
|---|---|---|
| 基准尺寸 | `--rx-base` = `BASE_PX(300) × scale` | 窗口尺寸恒为 `base × round(base × 1.6)` |
| 缩放进度 | `--rx-scale`（0.6–2.5） | 滑条拖动 → 宠物窗 `applyWindowSize()` 同步窗口 |
| 水平镜像 | root 加 `.rx-mirror` → `transform: scaleX(-1)` | **只能翻一次**；气泡文字用 `.rx-root.rx-mirror .rx-text{...scaleX(-1)}` 反向补偿保持可读 |
| 呼吸动画 | `@keyframes rx-breathe`（`scaleY 1→1.03`）× `breatheMs` | 由 `.rx-anim` 类控制；`prefers-reduced-motion` 时自动关闭 |
| 按压反馈 | `.rx-body` `transform` 过渡（`cubic-bezier(.34,1.56,.64,1)`） | 拖拽/点击时轻微形变 |
| 主题色 | 主色 `#203170`，纸色 `#FFFDF7`，提示灰 `#94a3b8` | 面板/菜单/气泡统一 |
| UI 窗铺满 | body 加 `.rx-ui-body`：dialog 铺满 `100vw×100vh`、无圆角、`::backdrop` 透明 | 去掉"网页弹框"观感（v0.3.0） |

### 2.6 行为状态机（表情与动作）

| 触发 | 表情键 → 图 | 备注 |
|---|---|---|
| 启动/待机 | `default` → `roxy0.png` | 空闲基线 |
| 单击（位移 < 5px，`CLICK_SQ=25`） | `happy` / `surprised` 随机二选一 | 附带一句台词 |
| 双击 | `angry` → `roxy4.png` | 行为四 |
| 任务进行中（轮询 `tasks`，`taskPollMs`） | `sleepy` 80% 概率，平时 20% | 工作态 |
| 随机犯困（`sleepyEverySec` 25–35s，`sleepyChance`） | `sleepy` | 任务进行中不犯困；频率低/中/高 = 0.05/0.3/0.6 |
| 任务完成 / 失败 / 新增 | 汇报台词（`reportLines.taskDone/taskFailed/taskAdded`，`%title%` 占位） | 由任务状态变化触发 |
| 随机小动作 `flick` | 轻微形变 | `flickEverySec` 10–20s，`flickChance` 0.2，`flickMs` 1200 |
| 定时器 | `startIdleTimers()` / `startIntervals()` / `stopAllBehavior()` | 关动画时全部停止 |

### 2.7 数据与持久化设计

- **默认配置**（随包）：`assets/roxy-config.json`
- **用户覆盖**：`%HOME%/config.json`，读取时 `deepMerge(默认, 用户)`；仅允许覆盖 `prefs / speech / behavior / expressions` 四个键
- **原子写**：写临时文件（`.tmp`）→ `rename` 覆盖；失败返回 `false`（接口回 `ok:false`）
- **任务**：`%HOME%/tasks.json`，`{tasks:[{id,title,status,date,created,updated,source}]}`
- **上传图**：`%HOME%/uploads/<slot>-<8位hex>.<ext>`，内存缓存 `userImageCache`
- **日志**：`%HOME%/crash.log`（启动环境信息 + 未捕获异常）、`%HOME%/selftest.log` 与 `%HOME%/selftest/*.png`（自检）

### 2.8 关键设计决策（防回退清单）

1. **preload 必须是 `.cjs`**：`type:module` + 沙箱下 ESM preload 静默失效（曾致拖拽失效）。
2. **拖拽用屏幕坐标增量**（`screenX/screenY`），视口坐标在窗口跟随时增量≈0。
3. **穿透无快捷键**：只能设置勾选/托盘取消（历史上快捷键已移除）。
4. **设置窗独立**：宠物窗不再放大弹面板；UI 窗复用同一份脚本，`?ui=` 进入 UI 模式。
5. **UI 窗没有 `root`**：所有 `root`/`bodyEl` 操作必须 `if (!root) return`；改动走 `pet-run`。
6. **镜像只翻一次**：root 翻转 + 仅文字反向（曾因图片二次翻转互相抵消导致"镜像没用"）。
7. **IPC 载荷净化**：preload 侧 `jsonSafe`（丢弃不可序列化值）+ `onlyString`；主进程侧 `safeSend` try/catch + 只接受纯对象。
8. **异常必落盘**：`uncaughtException`/`unhandledRejection` → `crash.log`（含版本/exe/数据目录）后再弹窗。
9. **系统集成要真机闭环**：NSIS 卸载残留（v0.3.3）证明"退出码 0 ≠ 做完了"，验证必须看终态。
10. **中文/含空格路径是一等公民**：打包版自启只注册 exe，不写含路径参数；启动信息写日志便于远程排障。

---

## 3. 功能文档

### 3.1 功能总表

| # | 功能 | 入口 | 说明 | 主要代码位置 |
|---|---|---|---|---|
| 1 | 悬浮宠物 | 应用启动 | 透明置顶无边框窗，底部对齐、位置记忆 | `main.js createWindow` + widget ⑤ |
| 2 | 拖拽移动 | 按住宠物拖动 | 屏幕坐标增量 → `pet-move-by` | widget ⑤ / main IPC |
| 3 | 行为与表情 | 单击/双击/随机/任务态 | 见 §2.6 状态机 | widget ③④ |
| 4 | 气泡台词 | 行为触发/任务事件 | 无口傲娇风格，可自定义；权重随机 | widget ⑨ + config `lines/reportLines` |
| 5 | 任务看板 | 右键→添加任务 / 设置→统计 | 黄色便签式添加、状态切换（待办/进行中/完成/失败） | widget ⑧ + `/tasks` |
| 6 | 数据统计 | 托盘→数据统计 / 右键→数据统计 | 今日四项计数 + 7 天趋势 + 环形图 | widget ⑧ + `/tasks` |
| 7 | 系统托盘 | 任务栏图标 | 双击=打开设置；右键菜单见 §3.3 | `main.js rebuildTray` |
| 8 | 设置面板·说的话 | 右键/托盘→设置→说的话 | 随机台词开关、自定义台词组（权重 + 多行） | widget ⑦ `tabSpeechHtml` |
| 9 | 设置面板·图片 | 设置→图片 | 5 槽位上传/预览/恢复默认（PNG/JPEG/WebP ≤2MB） | widget ⑦ + `/user-image` |
| 10 | 设置面板·行为 | 设置→行为 | 大小、表情动画、水平镜像、犯困频率、置顶、开机自启、透明度、鼠标穿透、保存 | widget ⑦ `tabBehaviorHtml` |
| 11 | 鼠标穿透 | 设置勾选 / 托盘勾选 | 点击穿透到桌面；托盘取消可关 | `main.js applyMouseThrough` |
| 12 | 窗口透明度 | 设置滑条 / 托盘档位 | 原生 `setOpacity`，20%–100%，不闪烁 | main `pet-set-opacity` |
| 13 | 置顶两档 | 设置 / 托盘 | 一直置顶（screen-saver） / 仅桌面置顶（普通窗口可盖住） | main `pet-set-top-mode` |
| 14 | 开机自启 | 设置勾选 / 托盘勾选 | 打包版注册 exe；状态以系统为准 | main `pet-set-autostart` |
| 15 | 大小缩放 | 设置→行为→大小 | 拖动即实时缩放（保持底部不动），0.6–2.5 倍 | widget ⑤ + main `pet-resize-keep-bottom` |
| 16 | 水平镜像 | 设置→行为 | 左右翻转角色朝向（气泡文字保持可读） | widget ② CSS + `applyMirror` |
| 17 | 表情动画开关 | 设置→行为 | 关闭后停止呼吸/小动作/犯困等全部动画 | widget ④ `applyBehaviorPrefs` |
| 18 | 崩溃日志与自检 | 自动 / `ROXY_SELFTEST=1` | 环境信息 + 异常栈落盘；自检跑通设置全链路并截图 | main `logCrash` / `runSelfTest` |
| 19 | 一键启动（开发态） | `start.bat` | 自动镜像 + 完整性检测 + 启动 | `start.bat` |

### 3.2 交互映射

| 用户操作 | 结果 |
|---|---|
| 左键单击宠物 | 随机"行为一/行为二" + 一句台词 |
| 左键双击宠物 | "行为四"（生气/发呆） |
| 按住拖动宠物 | 移动窗口（位置自动记忆） |
| 右键宠物 | 菜单：💬说话 / 📊数据统计 / ➕添加任务 / ⚙️设置 / ⏻退出 |
| 托盘左键双击 | 打开设置窗 |
| 托盘右键 | 打开设置 / 数据统计 / 鼠标穿透✓ / 透明度（20·40·60·80·100） / 置顶方式（一直置顶·仅桌面置顶） / 开机自启✓ / 退出 |

---

## 4. 接口文档

### 4.1 本地 HTTP API

- **基址**：`http://127.0.0.1:<随机端口>`（`startServer({port:0})` 返回 `{port, close}`；仅绑定回环地址）
- **通用约定**：请求/响应均 JSON（`Content-Type: application/json; charset=utf-8`，`Cache-Control: no-store`）；成功 `{"ok":true,...}`，失败 `{"ok":false,"code":"...","error":"..."}`
- **路由表**：未匹配路径**回落到 `/`**（返回 index.html）；各处理器内异常被 `safeHandler` 捕获 → `ok:false, code:"ERROR"`

| 方法 | 路径 | 参数 | 请求体 | 响应 | 说明 |
|---|---|---|---|---|---|
| GET | `/` | — | — | HTML | `index.html`（内含 `/widget.js` 引用） |
| GET | `/widget.js` | — | — | JS | `client/roxy-widget.js`；读取失败返 404 文本 |
| GET | `/config` | — | — | `{ok, config}` | 默认配置与用户覆盖深合并后的完整配置 |
| GET | `/prefs` | — | — | `{ok, prefs, speech, behavior, expressions}` | 配置中的四个可覆盖块 |
| PUT/POST | `/prefs` | — | `{prefs?, speech?, behavior?, expressions?}` | `{ok:true}` | 仅这 4 个键会被深合并写入 `config.json` |
| GET | `/tasks` | — | — | `{ok, today:{total,done,doing,failed}, trend:[7], tasks:[...]}` | `tasks` 按 `updated` 倒序 |
| POST | `/tasks` | — | `{title}` | `{ok, task}` | `title` 去空格、截断 80 字符；空标题 → `EMPTY_TITLE` |
| PATCH | `/tasks?id=<id>` | `id` | `{status}` | `{ok, task}` | `status ∈ todo\|doing\|done\|failed`，否则 `BAD_STATUS`；找不到 → `NOT_FOUND` |
| DELETE | `/tasks?id=<id>` | `id` | — | `{ok:true}` | 找不到 → `NOT_FOUND` |
| GET | `/image?name=<file>` | `name` | — | PNG/JPEG/WebP 字节流 | 仅包内白名单图（须出现在 `expressions` 中且名字匹配 `^[\w.-]+$`）；`Cache-Control: max-age=3600` |
| GET | `/user-image?name=<file>` | `name` | — | 图片字节流 | 仅"已被 expressions 引用"的用户图；`no-store` |
| POST | `/user-image` | — | `{slot, mime, data(base64)}` | `{ok, url, slot, fileName}` | 校验：slot 合法 / mime ∈ png,jpeg,webp / base64 可解 / 魔数一致 / ≤2.5MB；写入 `uploads/` 并替换该槽位旧图 |
| DELETE | `/user-image?name=<file>` | `name` | — | `{ok:true}` | 删除文件并清理 `config.json` 中引用 |

**示例**

```http
POST /user-image
{"slot":"happy","mime":"image/png","data":"iVBORw0KGgoAAAANSUhEUg..."}
→ 200 {"ok":true,"url":"/user-image?name=happy-1a2b3c4d.png","slot":"happy","fileName":"happy-1a2b3c4d.png"}

PUT /prefs
{"prefs":{"scale":1.3,"mirror":true}}
→ 200 {"ok":true}
```

**错误码表**

| code | 场景 |
|---|---|
| `BAD_JSON` | 请求体不是合法 JSON |
| `BAD_BODY` | `/prefs` 请求体不是对象 |
| `EMPTY_TITLE` / `BAD_ID` / `BAD_STATUS` / `NOT_FOUND` | 任务相关校验失败 |
| `BAD_SLOT` / `BAD_MIME` / `BAD_DATA` / `BAD_CONTENT` / `TOO_LARGE` / `WRITE_FAIL` | 图片上传相关 |
| `ERROR` | 处理器内部异常（`safeHandler` 兜底，错误文本截断 200 字符） |

### 4.2 IPC 通道（主进程 ↔ 渲染层）

| 通道 | 类型 | 载荷 | 主进程行为 |
|---|---|---|---|
| `pet-move-by` | renderer→main | `{dx,dy}` | 宠物窗位置增量移动 |
| `pet-resize-keep-bottom` | renderer→main | `{w,h}` | 尺寸夹取 `120–1600 × 160–1400`，保持底边、限制在工作区内 |
| `pet-window-set` | renderer→main | `{w,h}` | 同上但保持当前左上位置尽量不动（夹取进工作区） |
| `pet-set-top-mode` | renderer→main | `mode` | `'desktop'` → 关闭置顶（normal）；其它 → `screen-saver` 置顶 + 全工作区可见 |
| `pet-set-autostart` | renderer→main | `enabled` | 打包版：`{openAtLogin}`；dev：附加 `path=execPath, args=[__dirname]` |
| `pet-get-autostart` | renderer→main（invoke） | — | 返回 `app.getLoginItemSettings().openAtLogin` |
| `pet-set-mouse-through` | renderer→main | `enabled` | `setIgnoreMouseEvents(enabled,{forward:true})` + 广播 `through-state` + 刷新托盘 |
| `pet-set-opacity` | renderer→main | `value` | 夹取 `0.2–1`，作用于宠物窗，写 `trayState` |
| `pet-open-ui` | renderer→main | `kind` | `'settings'\|'tasks'`：打开/聚焦 UI 窗（单例） |
| `pet-run` | 双向 | `payload`（纯对象） | 广播给**除发送者外**的所有窗口；非纯对象直接丢弃 |
| `pet-state-sync` | renderer→main | `{topMode,opacity,mouseThrough,autostart}` | 更新 `trayState` 并重建托盘菜单 |
| `pet-quit` | renderer→main | — | `app.quit()` |

### 4.3 preload API（`window.petDesktop`）

```ts
interface PetDesktop {
  moveBy(dx: number, dy: number): void
  resizeKeepBottom(w: number, h: number): void
  setTopMode(mode: 'always' | 'desktop'): void           // 非字符串直接忽略
  setAutostart(enabled: boolean): void
  getAutostart(): Promise<boolean>
  setMouseThrough(enabled: boolean): void
  setOpacity(value: number): void                        // 0.2 ~ 1
  openUi(kind: 'settings' | 'tasks'): void               // 非字符串直接忽略
  sendRun(payload: object): void                         // 载荷经 JSON 净化，非法载荷丢弃
  onRun(cb: (payload: any) => void): void                // 接收广播指令
  stateSync(state: object): void                         // 同上净化
  onMouseThroughChange(cb: (v: boolean) => void): void
  quit(): void
}
```

### 4.4 配置数据契约

**包内默认：`assets/roxy-config.json`**（用户覆盖：`%HOME%/config.json`，深合并）

| 键 | 类型 | 字段 | 默认值 | 说明 |
|---|---|---|---|---|
| `expressions` | object | `default/happy/surprised/sleepy/angry` | `roxy0~4.png` | 表情键 → 图片文件名（包内白名单）或 `user-image/<file>` |
| `prefs` | object | `scale` | `1.0` | 0.6–2.5 |
| | | `mirror` | `false` | 水平镜像 |
| | | `animationOn` | `true` | 表情动画总开关 |
| | | `linesOn` | `true` | 台词（保留项） |
| | | `topMode` | `always` | `always` / `desktop` |
| | | `autostart` | `false` | 开机自启 |
| | | `opacity` | `1` | 0.2–1 |
| | | `mouseThrough` | `false` | 鼠标穿透 |
| `behavior` | object | `breatheMs` | `2400` | 呼吸周期 |
| | | `flickEverySec` | `[10,20]` | 小动作间隔区间（秒） |
| | | `flickChance` | `0.2` | 小动作概率 |
| | | `flickMs` | `1200` | 小动作时长 |
| | | `sleepyEverySec` | `[25,35]` | 犯困间隔区间 |
| | | `sleepyChance` | `0.3` | 犯困概率（设置里低/中/高 = 0.05/0.3/0.6） |
| | | `sleepyMs` | `5000` | 犯困持续 |
| | | `bubbleMs` | `5000` | 气泡停留 |
| | | `taskPollMs` | `1000` | 任务轮询周期 |
| `speech` | object | `toggles.randomLines` | `true` | 随机台词开关 |
| | | `customLines` | `[]` | `[{group,weight,items:[...]}]` |
| `lines` | array | `[{group,weight,items}]` | 4 组（kuudere/tsundere/desktop/task） | 随机台词池 |
| `reportLines` | object | `taskDone/taskFailed/taskAdded` | 见文件 | `%title%` 占位替换 |

### 4.5 数据文件与目录

| 路径 | 内容 | 写入时机 |
|---|---|---|
| `%HOME%/config.json` | 用户覆盖（prefs/speech/behavior/expressions） | 设置保存、托盘/设置指令 |
| `%HOME%/tasks.json` | 任务列表 | 任务增删改 |
| `%HOME%/window.json` | `{x,y,w,h}` 窗口位置尺寸 | 移动/缩放后 400ms 防抖 |
| `%HOME%/uploads/` | 用户上传图（`<slot>-<hex>.<ext>`） | 上传/删除 |
| `%HOME%/crash.log` | 启动环境信息 + 未捕获异常栈 | 每次启动 + 异常时 |
| `%HOME%/selftest.log`、`%HOME%/selftest/*.png` | 自检日志与截图 | `ROXY_SELFTEST=1` 时 |

### 4.6 环境变量

| 变量 | 作用 | 备注 |
|---|---|---|
| `ROXY_HOME` | 覆盖用户数据目录 | 两套变体共用该变量名 |
| `ROXY_SELFTEST=1` | 启动后自动跑自检（真实 IPC + 设置操作 + 截图 + 服务端回读） | 结果见 `%HOME%/selftest.log` |
| `ELECTRON_MIRROR` / `ELECTRON_BUILDER_BINARIES_MIRROR` | 国内镜像（npmmirror） | 打包/安装依赖时使用 |
| `ELECTRON_CACHE` / `ELECTRON_BUILDER_CACHE` | 指向项目内缓存目录 | 离线/加速打包 |

---

## 5. 边界文档

### 5.1 硬性限制（数值边界）

| 项目 | 限制 | 位置 | 越界行为 |
|---|---|---|---|
| 上传图片（客户端预检） | ≤ 2MB | widget `startUpload` | toast「图片超过 2MB 限制」 |
| 上传图片（服务端） | ≤ 2.5MB | `server.js` | `TOO_LARGE` |
| 请求体 `/user-image` | ≤ 4MB | `readBody` | 连接销毁 → `ok:false` |
| 请求体 `/prefs` | ≤ 256KB | `readBody` | 同上 |
| 请求体 `/tasks` | ≤ 16KB | `readBody` | 同上 |
| 任务标题 | ≤ 80 字符（去空格后非空） | `taskHandlers.create` | 截断 / `EMPTY_TITLE` |
| 任务状态 | `todo·doing·done·failed` | `TASK_STATUS` | `BAD_STATUS` |
| 缩放 `scale` | 0.6 – 2.5 | widget + 服务端 | 夹取 |
| 透明度 `opacity` | 0.2 – 1 | widget + main | 夹取 |
| 窗口尺寸 | 120–1600 × 160–1400 px | main IPC | 夹取并限制在工作区 |
| 表情槽位 | 固定 5 个 | `EXPR_KEYS` | 非白名单 slot → `BAD_SLOT` |
| 单击判定 | 位移² < 25（<5px） | widget `CLICK_SQ` | 视为拖拽 |
| 图片名白名单 | 包内 `^[\w.-]+$`；用户 `^[a-z0-9-]+\.(png\|jpg\|jpeg\|webp)$` | `expressionValueSets` | 404 / 拒绝 |
| 端口 | 随机空闲端口，仅 `127.0.0.1` | `startServer` | 不对外开放 |

### 5.2 安全边界

- **渲染层隔离**：`contextIsolation:true`、`nodeIntegration:false`，所有能力经 `preload` 白名单暴露
- **IPC 载荷净化**：preload `jsonSafe`（JSON 往返，函数/DOM/特殊对象被丢弃）、`onlyString`（字符串白名单）；主进程 `pet-run` 只接受纯对象、发送包 `try/catch`
- **路径穿越防护**：图片名先过白名单正则，再由 `expressions` 引用集合二次确认；`/image`、`/user-image` 只按名字查表，不接受路径
- **文件写入**：文件名服务端生成（`<slot>-<随机hex>.<ext>`），上传内容做魔数校验
- **网络**：运行时**零外部网络**（服务只监听回环）；联网仅发生在安装依赖/打包阶段（走镜像）
- **已知限制**：未做代码签名（SmartScreen 会提示）；无自动更新通道；无 CSP 头（页面仅本地回环）

### 5.3 平台与环境边界

| 维度 | 结论 |
|---|---|
| 操作系统 | Windows 10/11 x64（未适配 macOS/Linux） |
| 中文 / 含空格安装路径 | **已适配**（v0.3.2 起；打包版自启不写路径参数，启动信息入日志） |
| 数据目录中文路径 | 可用（`ROXY_HOME` 指向中文目录已自检通过） |
| 安装模式 | NSIS 可选目录；per-user 无需管理员，per-machine（/allusers）需要 UAC 提权 |
| 卸载 | 卸载器会自动结束运行中的应用；**运行中/关闭后卸载都应彻底删除目录**（v0.3.3 起，`installer.nsh` 修复空目录残留） |
| 多显示器 / 缩放 | 位置夹取基于 `getDisplayMatching().workArea`；未做 DPI 专项适配 |
| Electron 沙箱 | 主进程默认沙箱行为；**DSH 沙箱内无法打包**（app-builder 需管道 spawn），需完整权限终端 |

### 5.4 行为边界与降级

| 场景 | 表现 |
|---|---|
| 包内配置读取/解析失败 | 使用 `FALLBACK_CONFIG`（内置最小配置），应用仍可用 |
| 包内图片缺失/读取失败 | `/image` 返 404；前端 `img.onerror` 回落到 `roxy0.png` |
| 服务端处理器异常 | `safeHandler` → `{ok:false, code:'ERROR'}`，前端 toast「保存失败」 |
| 主进程发送失败（载荷/窗口已销毁） | `safeSend` 捕获 → 写入 `crash.log`，不崩溃 |
| 主进程未捕获异常 | 落盘 `crash.log`（含版本/exe/数据目录）+ 弹窗，应用继续运行 |
| 宠物窗不存在时收到指令 | 指令被忽略（`sendToPet` 判空） |
| UI 窗里执行宠物专属逻辑 | `applyMirror/applyBehaviorPrefs/applyWindowSize` 检测 `root === null` 直接返回 |
| 无 `window.petDesktop`（纯浏览器） | 所有 IPC 调用有 `desktop &&` 守卫；窗口相关功能退化但界面可调试 |

### 5.5 并发与多实例

- **未实现单实例锁**：允许同时开多个实例（含两套变体各开一个）
- 多实例共享同一 `%HOME%`（同变体内）→ `config.json/tasks.json` 后写覆盖先写；**不同变体已用不同数据目录隔离**
- 同一变体重复安装同 `appId` → NSIS 视为升级（覆盖安装）；不同变体 `appId` 不同 → 可共存

### 5.6 明确不支持 / 未实现

- 代码签名、自动更新、崩溃上报服务、GitHub Release 自动发布
- 托盘"隐藏宠物"（宠物窗即本体，托盘只提供菜单）
- 壁纸级挂件（"仅桌面置顶"只是关闭普通置顶的近似实现）
- 多语言 i18n（安装器与界面均为中文）、macOS/Linux 适配
- 自定义台词的多语言/表情符号校验、任务看板编辑标题（仅增删与状态切换）

---

## 6. 开发文档

### 6.1 环境准备

1. Windows + Node.js（已验证 v24.19.0）+ npm 11
2. `npm install`；若 npm 的 allow-scripts 拦截了 postinstall，需手动执行一次 `node node_modules\electron\install.js`
3. 国内网络建议设置镜像（见 §6.2），缓存目录指到项目内以便复用

### 6.2 常用命令

```powershell
# 开发态启动（等同双击 start.bat）
npm start

# 静态检查（4 个 JS 文件语法）
npm run check

# 服务端冒烟测试（19 项，见 §6.4）
npm run smoke

# 打包（Windows NSIS 安装包）
$env:ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"
$env:ELECTRON_BUILDER_BINARIES_MIRROR="https://npmmirror.com/mirrors/electron-builder-binaries/"
$env:ELECTRON_CACHE="<项目根>\.electron-cache"
$env:ELECTRON_BUILDER_CACHE="<项目根>\.builder-cache"
node_modules\.bin\electron-builder.cmd --win

# 打包版自检（真实跑一遍设置全链路 + 截图）
$env:ROXY_SELFTEST="1"; $env:ROXY_HOME="<临时目录>"; .\dist\win-unpacked\<app>.exe
```

### 6.3 打包与发布流程

1. 改版本号：`package.json` 的 `version`（当前 0.3.4）
2. 打包（上节命令）→ 产物 `dist\<artifactName>-<version>.exe` 与 `dist\win-unpacked\`
3. 产物校验：exe 属性→详细信息（ProductName / ProductVersion / 描述）
4. 真机回归（§6.4 清单）→ 通过后再分发
5. **命名与共存**：换角色/换名需同时改 `name`、`appId`、`productName`、`artifactName`、数据目录默认值（见 §7.2）
6. **磁盘占用**：`.electron-cache/.builder-cache/node_modules` 可保留以加速后续打包；`dist\win-unpacked` 被占用会导致打包失败——此时把 `build.directories.output` 临时改到 `.build-tmp/out` 打包后再拷回

### 6.4 测试策略

| 层级 | 内容 | 命令 |
|---|---|---|
| 静态 | 4 个 JS `node --check` | `npm run check` |
| 服务冒烟 | **19 项**：`/` 与 `/widget.js` 下发、config 结构（无计费字段、lines 4 组、reportLines）、`/image` 正常与路径穿越被拒、任务 CRUD（POST/GET 统计/PATCH×2/DELETE）、用户图上传→下发→删除、`PUT /prefs` 深合并生效 | `npm run smoke` |
| 打包版自检 | `ROXY_SELFTEST=1`：IPC 缩放 → 打开设置窗 → 切行为页 → 拖大小 → 关动画 → 开镜像 → 保存 → 服务端回读 → 截图；零异常判定 | 见 §6.2 |
| 真机回归 | 安装→启动→设置各页签（说话/图片/行为）→保存→重启保持→托盘各开关→拖拽/缩放/镜像→卸载彻底 | 人工清单（见交接文档 §9） |

### 6.5 调试与排障

| 症状 | 首选排查 |
|---|---|
| 打包版"设置操作报错/白屏" | 读 `%HOME%/crash.log`（首段是启动环境信息：版本、exe、appDir、数据目录） |
| 设置控件无效 | 确认该改动是否走了 `pet-run` 指令、宠物窗是否在运行；UI 窗里禁止直接操作 `root` |
| 镜像/缩放无视觉变化 | 检查 CSS 变换是否被叠加两次（单次原则）、`--rx-base/--rx-scale` 是否同步到宠物窗 |
| 打包失败 `spawn EPERM` | 沙箱限制，需完整权限终端（见 §5.3） |
| 打包失败 `app.asar 被占用` | 关闭正在运行的解包版应用，或改用 `.build-tmp/out` 输出 |
| 卸载后目录残留 | 确认安装包含 `installer.nsh`（`build.nsis.include`） |
| 远程用户问题 | 先要 `crash.log`（含环境信息），必要时让用户以 `ROXY_SELFTEST=1` 复现 |

### 6.6 代码约定（提交前自检）

1. 触碰 `root/bodyEl` 前先判断运行形态（宠物窗/UI 窗/纯浏览器）
2. 跨窗口改状态：**只发指令**（`sendRun`），不要跨窗操作 DOM
3. 新增 IPC：preload 侧保持"只收可 JSON 化数据"，主进程侧 try/catch + 类型守卫
4. 保留内部文件名 `roxy*.png`、`client/roxy-widget.js`、环境变量 `ROXY_HOME`；只改"外部可见标识"
5. CSS 变换：同一元素同一轴**只翻一次**；需要文字可读的地方单独反向补偿
6. 版本号：每次可分发改动 +0.1（或补丁号 +0.0.1）；改版本号同时同步本文档 §0

### 6.7 Git 与协作

- **`.gitignore` 必守**：`node_modules/ dist/ .npm-cache/ .electron-cache/ .builder-cache/ .build-tmp/ *.log`
- **push 前清单**：`git ls-files` 无依赖/产物/缓存目录、无 >50MB 文件、无个人路径与邮箱
- **推送通道**：DSH 执行环境 HTTPS 出站被拦（schannel），GitHub 操作请在 VSCode / 普通终端完成；本地提交、清理、验证不受影响
- 提交信息：`fix: …` / `feat: …` + 说明 + 版本号（例：`fix: horizontal mirror had no visible effect …; bump to 0.3.4`）
- 两套变体：结构性修复**两边同步**；变体专属改动（图片、名称）只在其仓库内

---

## 7. 扩展文档

### 7.1 换一套角色图（最常用）

1. 准备 5 张**透明 PNG**，按槽位语义对应：`0=默认站姿`、`1=开心`、`2=惊讶`、`3=工作犯困`、`4=生气/发呆`
2. 覆盖 `assets/roxy0.png ~ roxy4.png`（**文件名不要改**，`roxy-config.json` 与前端白名单都按此名）
3. 覆盖 `ico/favicon.ico`（建议 ≥256×256，托盘/窗口/exe/安装包图标共用）
4. `npm run check` → 打包 → 装后确认：托盘图标、exe 图标、设置→图片页 5 个槽位预览、默认姿态

> 若某角色只有一种形象：把 `assets/roxy-config.json` 的 5 个 `expressions` 值全部指向同一张图即可。

### 7.2 新建一个变体（如 Elysia）

```
① 复制项目目录（排除 .git / dist / 各类缓存；node_modules 建议用支持长路径的复制方式）
② packages.json 改四处：name / build.appId / build.productName / build.artifactName
③ 数据目录隔离：lib/server.js 与 main.js 中的默认 %HOME% 目录名
④ 换 assets 图 + ico 图标（见 §7.1）
⑤ 可选：可见文案（托盘 tooltip、窗口 title、设置/统计标题、空态提示、index.html <title>）
⑥ npm run check → 打包 → 自检 → 真机回归
```

**注意**：批量文本替换时避免误伤内部标识（`roxy*.png`、`__roxyDesktopPet`、`ROXY_HOME`、`client/roxy-widget.js`）；PowerShell 传参不要把"成对数组"嵌套传递（历史上曾因此把整文件的 `<`/`.` 误替换，需从原项目恢复）。

### 7.3 新增表情 / 行为

1. 图放进 `assets/`（名字进白名单：`^[\w.-]+$`）
2. `assets/roxy-config.json` 的 `expressions` 增加键（同时 `EXPR_KEYS`、`EXPR_LABELS`、设置页 5 槽位网格按需扩展）
3. 在 widget ③（表情系统）加 `setExpression('<键>', 时长)` 的触发点；④（行为状态机）里挂计时器/概率
4. 服务端 `expressionValueSets()` 会把新键自动纳入白名单（无需改代码）
5. 冒烟测试可加一条：`GET /image?name=<新图>` 返回 200

### 7.4 新增一个设置项

1. 在对应页签 HTML（`tabSpeechHtml/tabImagesHtml/tabBehaviorHtml`）加控件（给唯一 `id`）
2. 在 `bindSettingsTab()` 里绑定事件：
   - 立即生效类（窗口级）：直接 `desktop.setXxx()`（宠物窗/UI 窗都可）
   - 角色显示类（需要宠物 DOM）：**UI 窗走 `sendRun({type:'…', value, save})`**，宠物窗直接调 `applyXxx()`
3. 在 `handleRun()` 增加对应 `type` 分支（宠物窗执行 + 可选 `persistPrefsQuick()`）
4. 在 `saveBehaviorTab()`（或对应保存函数）里纳入持久化；必要时 `sendRun({type:'reload-config'})`
5. 同步更新本文档 §3.1/§4.4，并在 `updateBehaviorControls()` 里支持跨窗口回填

### 7.5 新增跨窗口指令

`preload`（透传即可，无需改）→ 发送方 `desktop.sendRun({type:'my-type', …})` → 接收方（宠物窗）`handleRun()` 加分支 → 需要持久化时 `persistPrefsQuick()` / `apiFetch(API.prefs,{method:'PUT'})`。

### 7.6 新增 HTTP 接口

1. `lib/server.js` 的 `router` 加路径（异步用 `safeHandler` 包裹；注意未匹配路径会回落 `/`）
2. 入参校验走白名单/正则，写文件走 `atomicWriteJson` 或 `uploads/`
3. 前端加 `API.xxx` 常量与调用；`scripts/smoke-server.mjs` 加断言（保持"全绿"）
4. 更新本文档 §4.1 路由表与错误码表

### 7.7 新增托盘项

`rebuildTray()` 的 `Menu.buildFromTemplate([...])` 加条目 → 动作优先 `sendToPet({type:'…'})`（复用 handleRun）→ 勾选态从 `trayState` 读 → 渲染层用 `desktop.stateSync()` 回填。

### 7.8 主题与样式扩展

- 主色 `#203170`、纸色 `#FFFDF7`、分隔 `rgba(32,49,112,.12)`
- 关键类：`.rx-root/.rx-body/.rx-img/.rx-bubble/.rx-text`（宠物）、`.rx-dialog/.rx-tabs/.rx-panel/.rx-row`（面板）、`.rx-ui-body`（UI 窗铺满）
- 新增面板样式请复用 `.rx-*` 前缀，避免与宿主页面冲突
- UI 窗专属覆盖一律写在 `.rx-ui-body` 作用域内

### 7.9 打包层扩展

| 需求 | 做法 |
|---|---|
| 自定义卸载行为 | 编辑 `installer.nsh`（宏 `customUnInstall` / `customUnInit` / `customInstall` 等） |
| 安装器选项 | `package.json > build.nsis`：`oneClick`、`allowToChangeInstallationDirectory`、`createDesktopShortcut`、`deleteAppDataOnUninstall`… |
| 图标 | `build.win.icon`（ico 或 ≥256 的 png，自动转换） |
| 多目标（portable/zip） | `build.win.target` 增加 `portable`/`zip` |
| 打不进包 | 检查 `build.files` 白名单是否包含新增目录 |

---

## 8. 附录

### A. 文件与职责速查

| 文件 | 职责 | 关键导出/入口 |
|---|---|---|
| `main.js` | 主进程：窗口、托盘、IPC、自启、日志、自检 | `createWindow` / `openUiWindow` / `rebuildTray` / `applyMouseThrough` / `runSelfTest` |
| `preload.cjs` | 安全桥 + 载荷净化 | `window.petDesktop` |
| `lib/server.js` | 本地服务：配置/任务/图片/静态 | `startServer` / `readWindowState` / `writeWindowState` |
| `client/roxy-widget.js` | 全部界面逻辑（宠物 + UI 模式） | IIFE：①常量 ②DOM ③表情 ④行为 ⑤尺寸拖拽 ⑥任务轮询 ⑦菜单/设置 ⑧统计 ⑨气泡台词 |
| `assets/roxy-config.json` | 默认配置/台词/表情映射 | 被 `/config` 读取 |
| `installer.nsh` | 卸载收尾宏 | `customUnInstall` |
| `scripts/smoke-server.mjs` | 19 项服务冒烟 | `node scripts/smoke-server.mjs` |

### B. 常见故障速查

| 症状 | 原因 | 处理 |
|---|---|---|
| 拖拽无反应 | preload 变 ESM / 沙箱失效 | 确认 `preload.cjs` 为 CJS 且路径正确 |
| 宠物不动且视口增量≈0 | 用了视口坐标 | 改用 `screenX/screenY` 增量 |
| 设置改动无效果 | UI 窗里直接操作了 `root` 或未发指令 | 走 `sendRun` + 宠物窗 `handleRun` |
| 大小滑条拖动无效 | UI 窗无 `root` 抛异常（历史 bug） | 已在 v0.3.0 修：发 `scale` 指令 |
| 镜像无变化 | CSS 二次翻转抵消（历史 bug） | 已在 v0.3.4 修：只翻一次 |
| 打包 `spawn EPERM` | 沙箱管道限制 | 在完整权限终端打包 |
| 打包 `app.asar 被占用` | 解包版应用在运行 | 关闭实例或改输出目录 |
| 卸载后残留空目录 | NSIS 卸载器 CWD=安装目录（历史 bug） | 已在 v0.3.3 修（`installer.nsh`） |
| 别人机器主进程报错 | IPC 载荷/环境差异（历史 bug） | 看对方 `%HOME%/crash.log`；v0.3.1 起已加固 |

### C. 术语表

| 术语 | 含义 |
|---|---|
| 宠物窗 | 渲染角色本体的透明置顶窗口（`root` 存在） |
| UI 窗 | `?ui=settings\|tasks` 打开的 640×780 设置/统计窗口（**无 `root`**） |
| `%HOME%` | 用户数据目录（默认 `~/.roxy-desktop-pet`，变体 `~/.elysia-pet`，`ROXY_HOME` 可覆盖） |
| pet-run | 跨窗口指令通道（主进程广播，排除发送者） |
| 槽位 | `expressions` 的 5 个键（default/happy/surprised/sleepy/angry），对应 5 张角色图 |
| 变体 | 同一套代码 + 不同角色素材/名称/数据目录的发行包（Roxy / Elysia） |

### D. 变更记录

| 版本 | 摘要 |
|---|---|
| 0.1.0 | 首个可用安装包（功能完整：悬浮窗/托盘/设置/任务/上传） |
| 0.2.0 | 移除宠物右上角汉堡菜单按钮；打包元数据清理 |
| 0.3.0 | 修复设置面板控件失效（`root=null` 崩溃）+ 面板铺满窗口去"网页弹框"感 |
| 0.3.1 | 主进程 IPC 加固（载荷 JSON 净化 / `safeSend` / `crash.log`） |
| 0.3.2 | 中文目录适配、启动环境日志、内置自检 `ROXY_SELFTEST` |
| 0.3.3 | 修复卸载残留空目录（`installer.nsh`），运行中卸载实测通过 |
| 0.3.4 | 修复水平镜像无效果（CSS 二次翻转抵消） |
| 变体 | `elysia-pet`（爱莉希雅）同步至 0.3.4，独立 `appId`/名称/数据目录 |
| 文档 | v1.0（本文档）：合并设计/开发/扩展/接口/边界/功能六类文档 |
